# CADRE — Dépendances de développement
# ===========================================
# pip install -r requirements-dev.txt
# -------------------------------------------

-r requirements.txt

# --- Tests ---
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.5.0
coverage>=7.4.0
hypothesis>=6.95.0

# --- Qualité de code ---
black>=23.12.0
ruff>=0.1.0
mypy>=1.7.0
isort>=5.13.0

# --- Sécurité ---
bandit>=1.7.7
# pip-audit remplace `safety` (déprécié, exige un compte) — gratuit, PyPA/OpenSSF
pip-audit>=2.6.0
# trivy : scanner de vulnérabilités, installé via Docker/binaire (PAS un package PyPI)
# Voir : https://aquasecurity.github.io/trivy/latest/getting-started/installation/

# --- Documentation ---
sphinx>=7.2.0
sphinx-rtd-theme>=2.0.0
myst-parser>=5.1.0
sphinx-autodoc-typehints>=1.25.0
# Requis par myst_enable_extensions += ["linkify"] (docs/conf.py) -- régression
# (audit) : `make docs`/`sphinx-build` échouait (ModuleNotFoundError) sans lui,
# jamais déclaré ici alors que la doc l'active depuis le début.
linkify-it-py>=2.0.0

# --- Outillage ---
ipython>=8.18.0
ipdb>=0.13.0
pre-commit>=3.6.0
detect-secrets>=1.5.0
tox>=4.11.0

# --- Sigma CLI (optionnel, pour compilation) ---
sigma-cli>=0.6.0
